<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.how2java.pojo">
    <insert id="addCategory" parameterType="com.how2java.pojo.Category">
        INSERT INTO category_ (name) VALUES (#{name})
    </insert>

    <delete id="deleteCategory" parameterType="com.how2java.pojo.Category">
        DELETE FROM category_ WHERE id = #{id}
    </delete>

    <select id="getCategory" parameterType="java.lang.Integer" resultType="com.how2java.pojo.Category">
        SELECT * FROM category_ where id = #{id}
    </select>

    <update id="updateCategory" parameterType="com.how2java.pojo.Category">
        UPDATE category_ SET name=#{name} WHERE id = #{id}
    </update>

    <select id="listCategory" resultType="com.how2java.pojo.Category">
        select * from category_
    </select>
</mapper>